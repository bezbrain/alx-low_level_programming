#include <stdio.h>

/**
 * printing a statement without using the printf keyword
 * the function also returned zero
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
